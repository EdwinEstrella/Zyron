# Archivo histórico de migraciones

Las migraciones activas de Zyron viven exclusivamente en `supabase/migrations/` y se ejecutan con la CLI de Supabase. Este directorio conserva copias históricas para referencia y compatibilidad de auditoría; no debe usarse para `db push`, `db reset` ni despliegues.
