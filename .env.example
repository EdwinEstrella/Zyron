# Opcional: copia a .env en la raiz del proyecto (gitignored). Carga automatica con dotenv en main.js.
# Prioridad: archivos insforge.local.json / insforge.json; si no hay, se usan estas variables.

INSFORGE_BASE_URL=https://tu-proyecto.insforge.app
INSFORGE_ANON_KEY=tu_jwt_anon_de_insforge

# Opcional: logs detallados de IPC Insforge en el proceso principal (main.js). Por defecto desactivados.
# ZYRON_MAIN_VERBOSE=1
